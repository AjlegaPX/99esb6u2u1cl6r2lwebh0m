// config.js — что включено в приложении. Правьте этот файл, чтобы отключить или скрыть ненужное навсегда.
// Те же переключатели есть в панели «Настройки» внутри приложения; выбранное там хранится в браузере и имеет приоритет.
// false отключает блок целиком. Чтобы удалить функцию из кода совсем, см. README, раздел «Отключение и удаление функций».
// Идентификаторы состояний, структур и групп перечислены в data.js (CONDITIONS, STRUCTURES, GROUPS).
window.EYE_CONFIG = {
  features: {
    age: true,              // ползунок «Возраст пациента» и детский масштаб глаза
    childConditions: true,  // группа «Детский глаз» в проблемных местах
    parents: true,          // «Памятка для родителей»
    treatments: true,       // список «После лечения» и хирургические элементы
    callouts: true,         // выносные подписи с линиями к структурам
    normGhost: true,        // голубой контур нормы рядом с изменённой структурой
    elements: true,         // плашки и список элементов патологии (друзы, лоскут, мешок и т. п.)
    changes: true,          // блок «Что меняется» в описании проблемы
    patientView: true,      // окно «Как видит пациент»
    rays: true,             // ход лучей
    fly: true,              // автоматический подлёт камеры к проблеме
    labels: true,           // подписи структур на модели
    layersPanel: true,      // левая панель со слоями
    scenario: true,         // «Сценарий показа»: цепочка сохранённых состояний, проигрывается кнопками «Далее»/«Назад»
  },
  // Режим проблемы (панель «Показ» над сценой; значения с ползунков хранятся в браузере и имеют приоритет):
  //   context — видимость структур, не затронутых проблемой (0.15 = прозрачность 85 %, 0 = скрыты совсем);
  //   highlight — сила подсветки затронутого от 0 до 1; pvAuto — открывать окно «Как видит пациент» поверх модели при показе изменения.
  show: { context: 0.15, highlight: 0.7, pvAuto: true },
  // Готовые сценарии для всех устройств: вставьте сюда через запятую JSON, скопированный кнопкой «JSON» в панели «Сценарий показа».
  // Используются, пока в браузере нет своих сценариев.
  scenarios: [],
  hideConditions: [],       // идентификаторы состояний, которые не показывать: например ['rop', 'retinoblastoma']
  hideStructures: [],       // идентификаторы структур: например ['orbit_bone', 'tenon']
  hideGroups: [],           // группы структур целиком: 'shell', 'inner', 'vessels', 'nerves', 'muscles', 'adnexa', 'orbit'
};
